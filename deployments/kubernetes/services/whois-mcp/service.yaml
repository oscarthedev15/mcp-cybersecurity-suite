apiVersion: v1
kind: Service
metadata:
  name: whois-mcp
  namespace: mcp-platform
  labels:
    app: whois-mcp
    app.kubernetes.io/part-of: mcp-platform
spec:
  type: ClusterIP
  selector:
    app: whois-mcp
  ports:
    - port: 8009
      targetPort: 8009
      protocol: TCP

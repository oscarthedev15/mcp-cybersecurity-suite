apiVersion: v1
kind: Service
metadata:
  name: vulncheck-mcp
  namespace: mcp-platform
  labels:
    app: vulncheck-mcp
    app.kubernetes.io/part-of: mcp-platform
spec:
  type: ClusterIP
  selector:
    app: vulncheck-mcp
  ports:
    - port: 8013
      targetPort: 8013
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: shodan-mcp
  namespace: mcp-platform
  labels:
    app: shodan-mcp
    app.kubernetes.io/part-of: mcp-platform
spec:
  type: ClusterIP
  selector:
    app: shodan-mcp
  ports:
    - port: 8011
      targetPort: 8011
      protocol: TCP

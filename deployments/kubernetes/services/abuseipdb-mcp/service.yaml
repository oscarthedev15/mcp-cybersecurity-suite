apiVersion: v1
kind: Service
metadata:
  name: abuseipdb-mcp
  namespace: mcp-platform
  labels:
    app: abuseipdb-mcp
    app.kubernetes.io/part-of: mcp-platform
spec:
  type: ClusterIP
  selector:
    app: abuseipdb-mcp
  ports:
    - port: 8008
      targetPort: 8008
      protocol: TCP

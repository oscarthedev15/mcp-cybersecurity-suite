FROM python:3.12-slim

LABEL maintainer="oscarthedev15" \
      description="NVD/CVE MCP server (streamable-http transport)" \
      org.opencontainers.image.source="https://github.com/oscarthedev15/mcp-cybersecurity-suite"

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir uv

WORKDIR /app/servers/nvd-cve-mcp
COPY ./servers/nvd-cve-mcp /app/servers/nvd-cve-mcp

RUN uv sync --no-dev --frozen

ENV PATH="/app/servers/nvd-cve-mcp/.venv/bin:$PATH"

RUN groupadd -r mcp && useradd -r -g mcp -d /app -s /sbin/nologin mcp \
    && chown -R mcp:mcp /app
USER mcp

EXPOSE 8010

CMD ["python", "server.py"]
